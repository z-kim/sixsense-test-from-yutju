# 1. 테라폼 바이너리 및 플러그인 (가장 용량이 큼)
.terraform/
.terraform.lock.hcl

# 2. 인프라 상태 파일 (보안 및 충돌 방지)
# 주의: 이 파일에는 DB 비밀번호 등 민감 정보가 평문으로 저장될 수 있습니다.
*.tfstate
*.tfstate.backup
*.tfvars
*.tfvars.json

# 3. 보안 키 및 인증서 (절대 금지)
*.pem
*.key
*.pub
.env

# 4. 로그 및 임시 파일
*.log
crash.log
success.txt
.terraform.tfstate.lock.info
